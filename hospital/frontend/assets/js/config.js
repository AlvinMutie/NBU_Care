// Frontend runtime configuration (no build tools)
// If the API runs on the same host, you can set this to "" and use relative paths.
window.NBU_CONFIG = {
  API_BASE: 'http://localhost:4000/api',
  UPLOADS_BASE: 'http://localhost:4000',
};

